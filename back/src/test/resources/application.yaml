spring:
  datasource:
    url: jdbc:h2:mem:dotacion;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false

app:
  company-name: Dotacion Test
  cors:
    allowed-origins: http://localhost:4200

gg:
  jte:
    developmentMode: true
